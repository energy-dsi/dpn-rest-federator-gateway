apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.demoRunnerServer.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.demoRunnerServer.name }}
spec:
  type: {{ .Values.demoRunnerServer.service.type }}
  selector:
    app: {{ .Values.demoRunnerServer.name }}
  ports:
    - port: {{ .Values.demoRunnerServer.service.port }}
      targetPort: {{ .Values.demoRunnerServer.service.port }}
      protocol: TCP
